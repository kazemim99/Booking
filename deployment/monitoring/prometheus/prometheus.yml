global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    cluster: 'booksy-production'
    environment: 'production'

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# Load rules
rule_files:
  - 'alerts.yml'

# Scrape configurations
scrape_configs:
  # Prometheus itself
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']

  # Node Exporter (system metrics)
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']

  # PostgreSQL metrics
  - job_name: 'postgres'
    static_configs:
      - targets: ['postgres-exporter:9187']

  # Redis metrics
  - job_name: 'redis'
    static_configs:
      - targets: ['redis-exporter:9121']

  # .NET API metrics — single modular-monolith host (if you add Prometheus metrics)
  - job_name: 'booksy-api'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['booksy-api:80']
    relabel_configs:
      - source_labels: [__address__]
        target_label: instance
        replacement: 'booksy-api'

  # RabbitMQ removed: CAP runs in-process (no broker).

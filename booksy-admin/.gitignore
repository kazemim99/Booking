# Logs
# Log output at the project root only. A bare "logs" also ignored src/views/logs/, so the
# Logs page was never committed and the CI build could not resolve it (2026-09-19).
/logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

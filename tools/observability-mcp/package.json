{
  "name": "asanrezerve-observability-mcp",
  "version": "1.0.0",
  "private": true,
  "description": "MCP server that lets an AI assistant (Claude Code, Claude Desktop) read AsanRezerve's logs, digest, overview, log levels and cache through the admin API.",
  "type": "module",
  "bin": {
    "asanrezerve-observability-mcp": "src/server.mjs"
  },
  "scripts": {
    "start": "node src/server.mjs",
    "test": "node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.1",
    "zod": "^4.1.0"
  }
}

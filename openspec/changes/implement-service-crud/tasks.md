## 1. Implementation

- [x] 1.1 `ComposerService.description`; api add/update/delete; repository methods + tests
- [x] 1.2 `ServicesCubit` add/update/remove mutations + tests
- [x] 1.3 `_ServiceFormSheet` (gating, prefill, minutes→hours split) + list add/edit/delete actions; `AppStrings`
- [x] 1.4 Widget tests (add gating/payload, edit prefill/round-trip, delete confirm); `flutter analyze` + full suite green; commit + push

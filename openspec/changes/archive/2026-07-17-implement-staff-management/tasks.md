## 1. Data

- [x] 1.1 `ProviderStaffMember` + firstName/lastName/phone; api `addStaff/updateStaff/removeStaff`; repository methods + tests

## 2. State & UI

- [x] 2.1 `StaffCubit` mutations (add/update/remove, reload-on-success) + tests
- [x] 2.2 `StaffFormSheet` (add/edit, required-name gating, confirm-guarded remove) + StaffView add action & row tap
- [x] 2.3 Home checklist `staff` item → `/more/staff`; `AppStrings`

## 3. Verification

- [x] 3.1 Widget tests: add flow, edit prefill, remove confirm, gating; `flutter analyze` + full suite green

## 1. Implementation

- [x] 1.1 `BusinessHoursCubit.addBreak/removeBreak` + cubit tests (add → save payload carries it; remove; closed-day no-op)
- [x] 1.2 Hours editor: deletable break chips + «+ استراحت» (two time pickers, start<end guard) on open days; `AppStrings`
- [x] 1.3 Widget tests: chip delete updates state, add affordance only on open days; `flutter analyze` + full suite green; commit + push

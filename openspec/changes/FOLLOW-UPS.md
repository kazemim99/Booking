# Follow-ups carried out of archived changes

> Working register. Every item here was a known, documented loose end inside a change that has since been
> **archived** — archiving must never be where a defect goes to die. Each row names where it came from, what
> it actually is, and which change is expected to absorb it.
>
> Created 2026-08-19 alongside the archive batch in `OPENSPEC-AUDIT-2026.md` §4 Step 1–3.
> Remove a row when its destination change exists and owns it explicitly.

## Open

| # | Item | Came from | Destination |
|---|---|---|---|
| 1 | **CLOSED 2026-09-09.** `FindOverlappingSlotsAsync` never filtered `ProviderAvailability.StaffId`: it returned every overlapping slot in the ORGANIZATION regardless of which member owned it. This turned out to be two defects of very different severity. **(a)** In `MemberBookabilityService` the day-level idempotency check therefore asked "does this DAY have slots for the whole organization?", which is true as soon as the first member is synced, so every member after the first generated ZERO availability and was silently unbookable — a salon could only ever book its first employee. Fixed 2026-09-08 with `HasSlotsForStaffOnDateAsync`. **(b)** The remaining half, closed now, was worse: the two callers that WRITE — `CreateBookingCommandHandler.MarkAvailabilityAsBookedAsync` and `RescheduleBookingCommandHandler.MarkNewAvailabilitySlotsAsBookedAsync` — marked EVERY overlapping Available slot as Booked, so booking one stylist at 10:00 consumed every colleague's 10:00 slot and the whole salon went dark after a single appointment. It was invisible until per-member schedules landed, because while only one member ever had slots, "all overlapping slots" and "this member's slots" were the same set. `FindOverlappingSlotsAsync` now takes a `staffId` (null = the whole salon, which is what the organization-direct booking path wants), and `BookableResource` carries a `SlotStaffId` — the membership id for a member, null for the organization — so the resolver decides the key rather than each handler guessing. The two RELEASE callers were already exact via their `slot.BookingId` re-check; `CancelBookingCommandHandler` deliberately stays unscoped because the only staff key it has is `Booking.StaffId`, which is the ORGANIZATION id for a direct booking and would match no slot. 6 real-Postgres integration tests (3 of which fail without the filter, verified by reverting it) + 7 resolver unit tests. | `fix-reschedule-membership-staff` §2.5; (a) fixed 2026-09-08, (b) fixed 2026-09-09 | closed |
| 2 | **Expired-JWT handling on SignalR is untested.** The `Token-Expired` header exists (`JwtAuthenticationExtensions.cs:52-56`) and anonymous rejection is proven, but no test presents a genuinely expired token, and none covers an established connection whose token expires mid-flight. | `harden-resource-authorization` §6.3 | `harden-test-suite-and-dependencies` |
| 3 | **Payment integration/architecture tests.** Commit-failure-after-gateway-success leaves a recoverable `Pending`; ZarinPal happy path + duplicate callback credits once; architecture test asserting no `IPaymentGateway` invocation inside a DB transaction scope. `Booksy.ArchitectureTests` is currently an empty stub with `NetArchTest` referenced and no rule enforced. | `payment-consistency-and-idempotency` §4.4–4.6 | `harden-test-suite-and-dependencies` |
| 4 | **EF owned-`Money` insert quirk.** Constructing + `Add`ing a `Payment` aggregate in a fresh scope throws `Unable to track 'Payment.PaidAmount#Money' … PaymentId is null`. Likely root cause of several pre-existing red payment integration tests; forced `PaymentDedupTests` to validate at the raw-SQL layer. | `payment-consistency-and-idempotency` findings | `harden-test-suite-and-dependencies` |
| 5 | **`ByProvider` server-derivation** (currently inert: defaulted false, controller-unset) and **`AddBookingNotesCommand`** not yet in the resource-ownership set. | `harden-resource-authorization` §2.2b, §2.3 | Small follow-up; fold into `harden-test-suite-and-dependencies` or its own change |
| 6 | **Refund→commission reversal entry.** A refund after commission was recognised at payout debits ProviderPayable (correctly reducing/negativing the balance), but posts no PlatformRevenue reversal entry for the commission portion. | `financial-ledger-and-settlement` residuals | `financial-reporting-and-clawback-policy` |
| 7 | **Payout clawback policy.** Negative balance → real transfer is gated off; safe default (block further payouts) is active. Needs a finance decision before enabling. | `financial-ledger-and-settlement` §4.2, audit P1-3 | `financial-reporting-and-clawback-policy` |
| 8 | **Ledger-backed financial reporting endpoints** (admin/finance scope). Additive; out of customer scope. | `financial-ledger-and-settlement` residuals | `financial-reporting-and-clawback-policy` |
| 9 | **Migrate `Money`/`Price` to EF Core complex types** once nullable complex types land (EF 10+). | `fix-aggregate-persistence-concurrency` §4.1 | `booking-data-and-migration-hygiene` (already assigned there) |
| 10 | **Booking `DateTime` timezone round-trip.** A booking seeded at 10:00 reads back as 13:30 — exactly the Tehran +3:30 offset — between `TimeSlot.Create` and materialisation. Does not affect reschedule correctness (both sides of every comparison go through the same conversion), so it was recorded rather than chased. **2026-09-09: root cause identified — this is #48**, the legacy Npgsql timestamp switch writing UTC instants as local time into `timestamp without time zone` columns. Not booking-specific; fix it there. **2026-09-11: closed with #48** — whose mechanism was itself corrected: the columns are `timestamptz` and the shift happened on READ (`Kind=Local`), not on write. | `fix-reschedule-membership-staff` §4 finding | closed with #48 |
| 11 | **Duplicate `ApiResponseMiddleware`** (`Core.Domain/Infrastructure/Middleware` and `Infrastructure.API/Middleware`) emitting a generic `"Request completed successfully"`, breaking two integration assertions that expect per-action wording. | `fix-reschedule-membership-staff` §4.4b | `fix-api-response-envelope` |
| 12 | ~~**Gallery main-image E2E stays skipped**, blocked on the `RefreshProviderToken` HTTP self-call to the retired standalone UserManagement service.~~ **Blocker removed 2026-08-19** — the self-call is replaced by `InProcessTokenService`. The `test.skip` in `provider-registration-realistic.spec.ts:69` has **not** been lifted yet: that needs a run against a live stack to confirm the gallery actually populates, which is the browser suite's job, not a static change. Un-skip and run it at the next full E2E pass. | `add-playwright-e2e`, `harden-e2e-test-coverage` §4 | Un-skip at next E2E run |
| 13 | **`frontend-e2e` CI workflow has not run with `globalSetup`.** Advisory workflow, not a deploy gate. | `harden-e2e-test-coverage` §7.2 | Verify on next CI run; no change needed |
| 14 | **On-device E2E sweep of the customer app** (guest browse → book with login-at-confirm → cancel → reschedule; cold start authenticated and guest). Blocked: no emulator, no `ios/`, and `flutter build apk` cannot complete while Google Maven 404s. | `customer-app-ux-redesign` §10.2 | Release checklist, not a code change |

| 15 | **Retarget the ServiceCatalog/UserManagement integration suites to `Booksy.Host`.** *(Correction, 2026-08-19: `OPENSPEC-AUDIT-2026.md` and `COMPLETION_ROADMAP.md` both describe this as "~46 red tests to triage". Checked directly — `Booksy.ServiceCatalog.IntegrationTests` **compiles cleanly** (0 errors, 99 files). The real defect is that it boots `WebApplicationFactory<Startup>` with `Startup` resolved from `Booksy.API`, not `Booksy.Host`, so it starts the wrong entry point for a modular monolith. It is a retarget, not a triage.)*<br><br>**Known-good path:** `Booksy.Host.CompositionTests/HostCompositionFactory.cs` already boots the real host. It cannot use `Program` as the marker because `Booksy.Host` and `Booksy.UserManagement.API` both declare a global `Program` (CS0433), so it parameterises `WebApplicationFactory<>` on a Host-assembly *type* instead. The same trick applied as a one-line alias in `tests/Booksy.ServiceCatalog.IntegrationTests/GlobalUsing.cs` retargets all 99 files without editing any of them. Expect real failures afterwards (routes, auth, seeding) — that is the actual triage, and it needs Docker running for Testcontainers. | audit P1-1, ROADMAP Epic 3.1 | `harden-test-suite-and-dependencies` (step 9) |

| 16 | **`PUT /Providers/{id}/staff/{staffId}` returns 500.** `UpdateProviderStaffCommandHandler.cs` is commented out in its entirety (0 non-comment lines), so no handler is registered and `_mediator.Send` throws `InvalidOperationException` before the controller can return. The sibling DELETE defect was fixed on 2026-08-19; this one was deliberately left. **Needs a product decision, not just code:** "update a staff member" maps to three distinct membership operations — `StaffProfile.DisplayName` (unclaimed members only; a claimed member's name belongs to the Person, and the salon should not be able to rename someone's account), `ChangeRoles`, and the provides-services toggle — plus fields the membership model has no home for at all (`email`, `notes`, `biography`, `profilePhotoUrl`, `countryCode`, which the Vue `UpdateStaffRequest` still sends). Decide which of those a salon may edit before wiring anything. | found during §8.7 survey, 2026-08-19 | `refactor-identity-and-membership` §8.7 / §8.3 |
| 17 | **Vue `staff.service.ts` still posts the full legacy shape.** `CreateStaffRequest`/`UpdateStaffRequest` carry `firstName`, `lastName`, `email`, `countryCode`, `notes`, `biography`, `profilePhotoUrl` — most of which the membership model does not store. The read path is already membership-backed and returns a **mixed** id (MembershipId for members, legacy ProviderId for un-migrated sub-providers), so the frontend cannot assume either. Reconcile the request/response contracts as part of §8.7 rather than leaving the client sending fields that are silently dropped. | found during §8.7 survey, 2026-08-19 | `refactor-identity-and-membership` §8.7 |

| 18 | **`AuthController.GenerateToken` mints a 15-hour token but reports `ExpiresIn = 900`.** It passes `15` to `IJwtTokenService.GenerateAccessToken`'s `int expirationHours` parameter while commenting "15 minutes expiration", and `JwtTokenService` does `DateTime.UtcNow.AddHours(expirationHours)`. Every other call site (`AuthenticateUser`, `CompleteCustomerAuthentication`, `CompleteProviderAuthentication`, `RefreshToken`) passes 24. So this endpoint issues a token living **60x longer than it tells the client**, and clients that trust `ExpiresIn` will refresh far more often than needed while the old token stays valid for 15 hours. The in-process replacement (`InProcessTokenService`) does not reproduce this, but the endpoint itself is unchanged and still reachable. Consider changing the interface to take a `TimeSpan` so the unit is not guessable. | found while building the in-process token seam, 2026-08-19 | needs an owner — security/auth hygiene |
| 19 | **CLOSED, 2026-09-07 — third loopback retired.** `InvitationRegistrationService`'s `CreateUserWithPhoneAsync`/`DeleteUserAsync` no longer POST/DELETE `/api/v1/users` over `IHttpClientFactory`. New narrow port `IPersonAccountProvisioningService` (ServiceCatalog.Application), implemented in-process as `InProcessPersonAccountProvisioningService` (`Booksy.Host.Composition`, same pattern as the two token loopbacks): create dispatches into `IPersonProvisioningService.GetOrCreateByPhoneAsync` (the canonical guarded "one person per phone" path this platform already has — the HTTP call was re-deriving the same thing worse), delete soft-deletes via `User.SetStatus(UserStatus.Deleted)` (the same convention every phone/email uniqueness check already excludes). **The compensating-delete semantics turned out to be worse than "needs thinking through" — they were never reachable at all**: the target endpoint required `SysAdminOnly` (this flow's caller never carries it) *and* `DeleteUserCommand` has no MediatR handler registered anywhere in the codebase, so the call could only 401 or 500. Both were swallowed by the caller's own try/catch and logged as a warning, so every register-and-accept that created an account and then failed a later step has been silently orphaning that account — permanently claiming its phone number, since only a Deleted-status row is excluded from the uniqueness checks. **A second, related defect found and fixed along the way**: the in-process create path itself did not persist without a fix — `IPersonProvisioningService.GetOrCreateByPhoneAsync` only tracks the change (`SaveAsync`), and a ServiceCatalog command handler's `TransactionBehavior` only commits `IServiceCatalogUnitOfWork`, never `IUserManagementUnitOfWork` — the same class of gap already found and fixed in `CompleteProviderAuthenticationCommandHandler` for the same reason. Fixed by committing `IUserManagementUnitOfWork` explicitly inside the new adapter. 5 new composition tests against a real Postgres Testcontainer (DI resolution + scoping, create genuinely persists and is findable, delete genuinely soft-deletes and frees the phone number, delete on an unknown person reports false rather than throwing). Full unit suite (832) + architecture (8) + composition (20): green. | `refactor-identity-and-membership` §8.8, 2026-09-06; fixed 2026-09-07 | closed |

| 20 | **CAP's storage initializer targets the wrong database in integration tests.** `AddCapEventBus` reads `configuration.GetConnectionString("DefaultConnection")` during *service registration* inside `Program.cs`, which runs before `WebApplicationFactory`'s `ConfigureAppConfiguration` is applied — so CAP keeps whatever the appsettings on the content root say and logs "Initializing the storage structure failed!". *(Corrected: the file in the test output is `Booksy.Host`'s own `appsettings.json`, byte-for-byte — not a collision with the referenced `ServiceCatalog.Api` copy as first suspected. The Host's own dev config carries `Port=54321`, so this is purely a configuration-ordering problem, which makes `UseSetting` a complete fix rather than a workaround.)*. EF is unaffected only because `TestWebApplicationFactory` replaces `DbContextOptions` explicitly. Any test depending on CAP outbox behaviour is therefore untrustworthy. Likely fix: set the connection string via `IWebHostBuilder.UseSetting`, which lands before the app builder reads configuration, rather than via `ConfigureAppConfiguration`. `Booksy.Host.CompositionTests` does not hit this because it references only `Booksy.Host`. | integration-suite retarget, 2026-08-19 | `harden-test-suite-and-dependencies` |
| 21 | **Stale integration tests assert removed request shapes.** `UpdateBusinessInfo_WithInvalidPhoneNumber_ShouldReturn400BadRequest` posted `PhoneNumber`/`Email`/`OwnerFirstName`/`OwnerLastName`, none of which exist on `UpdateBusinessInfoRequest` (BusinessName/Description/LogoUrl only) — model binding discarded them and 204 was correct. Rewritten on 2026-08-19 into two tests against fields the endpoint actually declares. **Others of this kind are likely**: when triaging a failure in this suite, first confirm the test's payload matches the current request model before treating it as a defect. | integration-suite retarget, 2026-08-19 | `harden-test-suite-and-dependencies` |
| 22 | **`UpdateProviderStaffCommandHandler`, `DeactivateProviderStaffCommandHandler` and `UpdateBusinessInfoCommandHandler` were all fully commented out.** The second was fixed on 2026-08-19 (#16 covers the first). `UpdateBusinessInfoCommandHandler` is dead but harmless: the controller dispatches the live `UpdateBusinessProfileCommand` instead, so the commented file is an orphan that should simply be deleted along with its unused command. Worth a sweep for other 0-live-line handler files. | found during §8.7 and the retarget, 2026-08-19 | cleanup |

| 23 | ~~**The entire ServiceCatalog validation layer is dead code — 34 validators, none registered.**~~ **RETRACTED, 2026-08-24 — the premise was wrong.** `ServiceCollectionExtensions.AddValidatorsFromAssembly` does scan `Booksy.Core.Domain` only, but `AddMediatorWithBehaviors` (in `Booksy.Infrastructure.Core/CQRS/MediatorExtensions.cs`, called by `AddServiceCatalogApplication` for every command/query) **separately** calls `services.AddValidatorsFromAssemblies(AppDomain.CurrentDomain.GetAssemblies())` — every loaded assembly, which includes `Booksy.ServiceCatalog.Application`. Proven empirically: `CreateZarinPalPaymentCommandValidator` genuinely fires in a running integration test (traced through `ValidationBehavior` → `DomainValidationException` → 400, for a scenario that should have passed FluentValidation cleanly). **The original claim was reached by grepping one file** (`ServiceCatalogApplicationExtensions.cs`) for a direct `AddValidatorsFromAssembly` call and stopping there, without tracing what `AddMediatorWithBehaviors` — called from that same file — actually does. Every decision gated on "validators are off" in this session (the domain-level length guard added to `BusinessProfile.UpdateGalleryImageMetadata`, the caution around not "activating" anything) was gating on a false premise: **there was never an activation step pending.** The domain guard is not wrong — it is legitimate defense-in-depth — it is simply redundant with a validator that was already running. No code change follows from this retraction; it is a correction to the audit record. | integration-suite retarget, 2026-08-20; retracted 2026-08-24 during gateway feature triage | closed — see #31 for what actually blocks the gateway suite |
| 31 | **No `IZarinPalService`/`IBehpardakhtService`/`IPaymentGateway` mock is ever registered with the test factory's DI container, so every Reqnroll scenario that reaches a payment command makes a genuine outbound HTTP call to the real ZarinPal/Behpardakht API.** In this environment that fails with ZarinPal's own error — `"The merchant id must be at least 36 characters"` — because `Payment:ZarinPal:MerchantId` is still the placeholder `"your-zarinpal-merchant-id"`, the exact same credential gap `checkout-release-gates` already tracks for the Flutter checkout flow (T3). **Broader than first scoped**: it is not limited to the 10 ZarinPal/Behpardakht-named feature files. `ProcessPayment.feature`, `RefundPayment.feature` and `PaymentCommands.feature` — all inventoried as fully bound, S/M effort, "no unbound steps" — fail identically, because `ProcessPaymentCommand`/`RefundPaymentCommand` route through the same real gateway underneath regardless of which feature file exercises them. `ZarinPalSteps`/`BehpardakhtSteps` do construct a `Mock<IZarinPalService>`/`Mock<IBehpardakhtService>` and configure `.Setup(...)` on it, but never register it — so the mocks are inert; MediatR resolves the real implementation every time. This is the actual reason the whole payment-command surface cannot pass here — not missing step definitions, not validators (§23, retracted), not a defect in the scenarios themselves. The effort estimates in `REQNROLL-COVERAGE-GAP.md` for `ProcessPayment.feature`, `RefundPayment.feature`, `PaymentCommands.feature` and all ten ZarinPal/Behpardakht files need revising: they are credentials-blocked, not harness-gapped. **Secondary, smaller defect found along the way (real, but currently unreachable without live credentials):** the app's own error-response DTO (`List<ZarinPalError>`) does not match ZarinPal's actual error shape (a single object, not an array) — `System.Text.Json.JsonException` when the real gateway does return an error. | domain-expert feature audit, 2026-08-24 | wire the mocks into DI via `ConfigureTestServices` (replace `IZarinPalService`/`IBehpardakhtService`/`IPaymentGateway`), or accept these scenarios as gated the same way as `checkout-release-gates` and mark them `@requires-credentials` |
| 32 | **`BookingCommands.feature`'s reschedule scenarios fail on `"Request completed successfully"` not containing `"rescheduled successfully"`.** Not a regression and not new — this is FOLLOW-UPS #11 (the duplicate `ApiResponseMiddleware`) reaching a feature file it hadn't been checked against yet. `Bookings/RescheduleBooking.feature` (the file already domain-audited this session) is unaffected because its assertions don't check response wording. | domain-expert feature audit, 2026-08-24 | `fix-api-response-envelope` (already owns #11) |
| 24 | **Remaining GalleryManagementTests failures (3 of 31), all in the reorder/ordering family.** (a) `ReorderGalleryImages_WithValidOrder` and (b) `SetPrimaryGalleryImage_PrimaryImageAppearsFirst` — the PUT returns 204 and the subsequent GET still shows the original order. **Ruled out so far:** the query ordering is correct and intentional (`OrderByDescending(IsPrimary).ThenBy(DisplayOrder)`); no image is auto-primary (`GalleryImage.Create` defaults `isPrimary:false` and `AddGalleryImage` never overrides), so ordering does fall through to `DisplayOrder`; the query is not cached (plain `IQuery`, no cacheable marker); the write repository does `Include(Profile).ThenInclude(GalleryImages)` so the children are loaded and tracked; `Dictionary<Guid,int>` body binding is supported; and both handlers now persist (see below). **Still unexplained:** why a tracked `DisplayOrder`/`IsPrimary` change on an owned child is not written, when a *removal* from the same collection is (delete persists — that test fails only on soft-vs-hard-delete semantics). Next step is an empirical one: assert on raw SQL immediately after the PUT to establish whether the new `DisplayOrder` reaches the table at all, which separates "not saved" from "not returned". Note this smells like the ADR-005 owned-entity family. (c) `DeleteGalleryImage_WithValidImage` expects to still find the image afterwards (soft-delete) but `RemoveGalleryImage` hard-deletes by design and says so — likely a stale test, but confirm the intended semantics before changing either side. | integration-suite retarget, 2026-08-20 | `harden-test-suite-and-dependencies` |

| 25 | **`GetUserByIdQueryHandler` dereferences `user.Profile` unconditionally, but a profile-less `User.Register` overload exists.** `User.Register(email, password, type)` leaves `Profile` null; the query handler then NREs on `user.Profile.FirstName`. Any user created through that overload breaks every read of that query — which, post-monolith, includes provider registration (it mints a provider token, which loads the user). Not hypothetical: it is what the integration fixture hit. Either the overload should populate a default profile, or the handler should tolerate a null one. | found while building cross-context test fixtures, 2026-08-22 | needs an owner — UserManagement robustness |
| 26 | **Provider registration rolls back if token minting fails.** `RegisterProviderCommandHandler` saves the provider and then mints a token carrying the new provider claims; the token service throws on failure and `TransactionBehavior` rolls the whole registration back. Pre-existing (the retired HTTP `TokenService` rethrew too, so the in-process seam preserved the contract exactly) and **not** what the tests were hitting, but it means a transient identity-side failure discards an otherwise-complete registration. Consider treating the token as a best-effort convenience — the client can always re-authenticate — rather than a precondition for the provider existing. | observed while triaging the cross-context registration failures, 2026-08-22 | needs a product/architecture decision |

| 27 | **`Payouts.feature` has no scenario covering ADR-007's refund-reduces-payout rule.** `CreatePayoutCommandHandler` correctly reads `GetProviderPayableBalanceAsync` (ledger-derived, confirmed in code), and ADR-007 states refunds debit ProviderPayable so they automatically reduce payouts. No scenario exercises this — the only calculation scenario present uses 5 clean payments with no refunds, which cannot distinguish ledger-derived from payment-summed math. A missing scenario, not a defect. | domain-expert feature audit, 2026-08-24 | needs a scenario written once `financial-reporting-and-clawback-policy` (FOLLOW-UPS #6/#7) is scoped |
| 28 | **`ProviderRegistration.feature` asserts a business-name-uniqueness rule that does not exist.** "Cannot register provider with duplicate business name" expects 400 + a duplicate-name error; `RegisterProviderCommandHandler`/`RegisterProviderCommandValidator` have no such rule anywhere (confirmed by full-text search of the validator body), and — #23 retracted — the validator genuinely does run, so this is a real gap in the rule itself, not an inactive-validator artifact. Not fixed: whether two providers may legitimately share a business name (plausible — "Beauty Salon" is a common name) is a product decision, not a bug to patch silently. | domain-expert feature audit, 2026-08-24 | needs a product decision: enforce uniqueness, or correct the scenario |
| 29 | **`GetProviderEarningsQueryHandler` sums `Payments` directly, contradicting ADR-007** ("all financial figures... derived from the ledger, never re-summed from Payments/Bookings"). Confirmed in code — no `LedgerEntry` read anywhere in the handler. Not a new finding: already covered by #8 (`financial-reporting-and-clawback-policy`). `Financial.feature`'s scenario numbers (15% commission, 500/75/425) are internally consistent with the current implementation, so the feature content itself needs no correction — only the underlying handler, which is the tracked decision. | domain-expert feature audit, 2026-08-24 | `financial-reporting-and-clawback-policy` (already tracked as #8) |

| 30 | **`LocationResponse.State` is hardcoded to `""` regardless of input.** `UpdateLocationController` accepts `ProvinceId`/`CityId` on the command but never resolves either into the response's `State` field — it is an unconditional empty string. Not fixed: resolving a province id to a display name is a lookup this controller doesn't currently make, and guessing at the right source would be exactly the kind of production change this audit is meant to avoid making without evidence. | domain-expert feature audit, 2026-08-24 | needs scoping — is State meant to be provider-editable free text, or derived from ProvinceId? |

| 33 | **Booking-index perf-smoke not verifiable in this environment.** `booking-data-and-migration-hygiene` §4.2 wanted an `EXPLAIN`-level proof that the restored `Bookings` indexes (`IX_Bookings_CustomerId`/`ProviderId`/`StaffId`/`Status`, migration `20260905083540_RestoreBookingIndexes`) are actually chosen by the query planner over a sequential scan. A fresh Testcontainers Postgres has no realistic row count to make that measurement meaningful — structural correctness (the indexes exist, migrate cleanly, and match the queries they're meant to serve) is verified; planner-choice effectiveness is not. | `booking-data-and-migration-hygiene` §4.2, 2026-09-05 | needs a real/staging-scale database to run `EXPLAIN ANALYZE` against |
| 34 | **`design-system-convergence` §6.3 (device/emulator visual QA) not done — no device available in this environment.** Same category of environment gap as the Android-toolchain blocker already tracked under `checkout-release-gates` §3.1, but for the general design-system migration rather than the checkout flow specifically: every migrated screen (wizard steps, dialogs, sheets, the animated category tiles and city chevron) passed its automated widget tests but has never been eyeballed by a human or a real renderer. | `design-system-convergence` §6.3, 2026-09-05 | needs a human or CI device farm to visually confirm the migrated screens |
| 35 | **A widespread pattern of authorization failures throwing `UnauthorizedAccessException` (→ 401) where the caller's own controller XML docs, or plain semantics, call for 403 Forbidden.** Confirmed and fixed for the three membership handlers this required (`TerminateMembership`/`RevokeInvitation`/`ChangeMembershipRoles`) and, **2026-09-07, for `DeleteProviderServiceCommandHandler`/`UpdateProviderServiceCommandHandler`** — and fixing the latter two surfaced something worse than a status-code bug: **a real IDOR**. `ServicesController`'s `PUT/DELETE {providerId}/{serviceId}` routes only required the `"ProviderOrAdmin"` policy (any authenticated provider) — unlike `ActivateService`/`DeactivateService`/`ArchiveService` on the *same controller*, which all call `CanManageService` first, and unlike `ProviderSettingsController`'s equivalent routes, which call `CanManageProvider(id)` first. The handler's own check (`service.ProviderId.Value != request.ProviderId`) only proved the pair was internally consistent — it never checked the *caller* owned that provider — so any provider could pass another provider's `providerId`/`serviceId` pair and edit or delete their service. Fixed at both the choke point (the two handlers now also verify `provider.OwnerId == callerId`, admin-bypassed, throwing the same `ForbiddenException`) and the specific route (added the missing `CanManageService` check to `ServicesController`, matching its own sibling actions) — belt and suspenders, since a future caller of either handler is protected even if a controller-level check is missed again. 7 new tests (4 controller-level in `ServicesControllerAuthorizationTests`, cross-tenant update/delete now correctly 403; 3 handler-level in a new `ServiceOwnershipAuthorizationTests`, including an admin-bypass case). Verified the pre-existing `ServiceManagement.feature` Reqnroll failures for these two scenarios are unrelated (`XUnitInconclusiveException`: unbound steps, the same pre-existing gap `REQNROLL-COVERAGE-GAP.md` already tracks — not a regression from this fix). **CLOSED, 2026-09-07** — the remaining six were checked individually rather than assumed safe to batch-fix: `DeactivateProviderStaffCommandHandler` (×2 — membership-path and legacy-sub-provider-path), `CompleteProviderRegistrationCommandHandler`, `SaveStep4ServicesCommandHandler`, `SaveStep6WorkingHoursCommandHandler`, `SaveStep8FeedbackCommandHandler`, `SaveStep9CompleteCommandHandler`. All six turned out to have the **safe shape** (a complete, correct `provider.OwnerId != userId` or equivalent ownership check already present, immediately before the wrong-typed throw) — none had the `ServicesController`-style *missing* check, so none needed a new authorization decision, only the same exception-type swap. Fixed all six to `ForbiddenException`. Tests: `DeactivateProviderStaffCommandHandlerTests`'s existing stranger-case assertion corrected; a new `ProviderRegistrationAuthorizationTests` (5 tests — one per registration-step handler, none of which had any test before) pins the 403 at the handler boundary for each. Full unit suite (832) + architecture (8): green throughout. This closes the audit opened by this finding — every handler the original grep turned up has now been individually checked and fixed. | found via full-repo grep while fixing `refactor-identity-and-membership` §5.5, 2026-09-06; IDOR found and fixed 2026-09-07; remaining six checked and fixed 2026-09-07 | closed |

| 36 | **CONFIRMED: no database-level constraint backs the "one person per phone" invariant — two concurrent sign-ins for the same brand-new phone create two separate accounts.** `refactor-identity-and-membership` §1.9 asked for this to be verified; it was not previously known to be real. `PersonProvisioningConcurrencyTests.Concurrent_Customer_And_Provider_Signin_For_A_Brand_New_Phone` (new, `Booksy.UserManagement.IntegrationTests`) races a customer sign-in and a provider sign-in for one never-before-seen phone number, using two fully independent DbContext/repository/service instances against a real Postgres — reproduced 3/3 runs: both succeed, and the database ends up with two distinct user rows for the same phone. Root cause: `GetOrCreateByPhoneAsync`'s "no existing row" check is a plain SELECT with no locking or constraint behind it, so two requests that both observe "nobody has this phone yet" before either commits both proceed to INSERT. **Deliberately left as a skipped test, not fixed here**: the real fix is §1.2 (a partial unique index on `users."PhoneNumber"`), which cannot be added blind — it auto-applies via `Database.Migrate()` at host startup and would fail to apply, blocking every future deploy, if the real environment already holds duplicate phone numbers (§1.3). Added `scripts/find-duplicate-phone-numbers.sql` — a read-only report (exact duplicates, cross-format duplicates via the schema's own `NationalNumber` column, and profile phones never backfilled onto the account) to run against the real environment first; validated end-to-end against a throwaway Postgres container seeded with a synthetic cross-format duplicate pair, which it correctly found. Once someone runs that script against the real environment and confirms it is clean, §1.2's migration can be added and this test's `[Fact(Skip = ...)]` removed. | `refactor-identity-and-membership` §1.9, 2026-09-08 | `refactor-identity-and-membership` §1.2/§1.3 — needs real-environment access to run the report, then the migration |
| 37 | **CLOSED 2026-09-09** — `UserManagementDbContextOptions.Configure` is now the single options definition used by DI and both raw-context tests (`UserRepositorySaveTests`, `PersonProvisioningConcurrencyTests`); the tests pass unchanged. The diagnosis below was right: the bare `UseNpgsql` in the tests lacked the `PendingModelChangesWarning` suppression DI applies. Original text: **Pre-existing, unrelated: `Booksy.UserManagement.IntegrationTests`' raw-DbContext test pattern is currently broken.** Found while building #36's test, not caused by it — the *existing* `UserRepositorySaveTests` (3 tests, no changes made to them) also fail, all with the identical error: `"The model for context 'UserManagementDbContext' has pending changes. Add a new migration before updating the database."` (EF Core 9's `PendingModelChangesWarning`, configured to throw). This is a false positive, not a genuine drift: `dotnet ef migrations has-pending-model-changes` against the real DI-composed app reports **no** pending changes. The discrepancy is specific to how these tests construct `UserManagementDbContext` directly (`new UserManagementDbContext(options, Substitute.For<ICurrentUserService>(), fakeClock, Substitute.For<IDomainEventDispatcher>())`) rather than through the app's own DI container — something about that construction path makes EF compute a different model fingerprint than the one embedded in the migration snapshot, but *what* specifically was not investigated (a separate, EF-internals-level question). `PersonProvisioningConcurrencyTests` (added alongside #36) uses the same construction pattern but calls `EnsureCreatedAsync()` instead of `MigrateAsync()`, which sidesteps the check entirely (appropriate for a throwaway per-fixture database that only needs the current schema, not migration history) — a workaround for the new test, not a fix for the two affected ones. | found while building `PersonProvisioningConcurrencyTests`, 2026-09-08 | needs an owner — likely a `ConfigureWarnings` fix in the shared test-context-construction helper, or switching `UserRepositorySaveTests` to the same `EnsureCreatedAsync` pattern |

| 38 | **Pre-existing, unrelated: all 12 tests in `CustomersControllerTests` (`Booksy.UserManagement.IntegrationTests`) fail with 404 instead of their expected status.** Found while running the full `Booksy.UserManagement.IntegrationTests` project to check #36/#37's expected pass/fail counts — not caused by, or related to, this session's changes (no production code touched this session affects `Customer`/`CustomersController`). Root cause: `UserManagementIntegrationTestBase.CreateTestCustomerAsync` builds a `Customer` aggregate with a brand-new random `UserId.From(Guid.NewGuid())` and persists only that `Customer` row — it never creates a matching `User` row. `GetCustomerProfileQueryHandler` (and, going by the failure list, every other customer-profile/favorites/preferences/booking-history handler) loads the `Customer` then does `_userRepository.GetByIdAsync(customer.UserId, ...)` and throws `InvalidOperationException("User not found...")` when that lookup misses — which the pipeline turns into a 404. Every test built on `CreateTestCustomerAsync`/`CreateAndAuthenticateAsCustomerAsync` therefore fails the same way. Not investigated further or fixed — likely fix is for the test helper to also create a real `User` row (mirroring how `RegisterAndAcceptInvitationTests` and `PersonProvisioningConcurrencyTests` construct real `User`s via `User.RegisterWithPhone`/`PersonProvisioningService`), but that touches a shared base class used across the whole `CustomersControllerTests` file and deserves its own change, not a drive-by fix. | found while running full regression for `refactor-identity-and-membership` §1.9, 2026-09-08 | needs an owner to fix `CreateTestCustomerAsync` (or the handlers, if a `Customer` without a backing `User` is meant to be valid) |

| 39 | **Fixed: `Booksy.UserManagement.IntegrationTests` had never sandboxed `ISmsNotificationService` — any test exercising a handler that actually sends an SMS tried a real network call.** Found while adding real-HTTP tests for §1.8's phone-number-change fix: `UserManagementTestWebApplicationFactory` sets `ASPNETCORE_ENVIRONMENT=Test`, but `Booksy.UserManagement.API` ships no `appsettings.Test.json`, so the host silently falls back to the un-suffixed `appsettings.json` — the PRODUCTION-shaped config (`Rahyab:SandboxMode = false`, no `Sms:SandboxMode` key at all). A test that reaches `SendVerificationCodeCommandHandler`/`SendPhoneVerificationCodeCommandHandler` therefore attempted a genuine outbound HTTPS call to the real Rahyab SMS gateway from inside the sandbox, which has no route out, and failed after ~90 seconds with an SSL/connection error instead of a fast, deterministic result. No prior test in this project had ever exercised that code path over real HTTP, which is why this was never noticed. Fixed by registering a `FakeSmsNotificationService` (always succeeds, no network, captures the last message sent per phone number so a test can recover an OTP code the same way a human reads it off their phone — the code itself is never persisted, only its SHA-256 hash) in `UserManagementTestWebApplicationFactory.ConfigureTestServices`, replacing a `// Example: Replace SMS verification service with fake` comment that had sat there unimplemented. | found while building §1.8's `PhoneNumberChangeTests` (integration), fixed same day, 2026-09-08 | none — fixed; worth knowing if a future `appsettings.Test.json` is ever added for this project, since it would need the same sandbox values `appsettings.Development.json` already carries |

| 40 | **Remove the `Provider.OwnerId` authorization fallback once owner memberships are backfilled everywhere.** Ownership is now read membership-first in `TerminateMembership`, `ChangeMembershipRoles` and `RevokeInvitation` (`callerMembership?.IsOwner == true \|\| callerId.Equals(organization.OwnerId)`). The `OwnerId` half is a migration-only fallback for organizations registered before owner memberships existed. `register-full` and the step-9 wizard now both create one, so only pre-existing rows are affected — they are covered by `backfill_memberships.sql` STEP 1, which has not been run against the real environment yet. Once it has, drop the `\|\| callerId.Equals(organization.OwnerId)` clause from all three handlers so membership is the single source of truth, and the ≥1-owner invariant cannot be bypassed by a stale `OwnerId`. | `vue-staff-on-memberships` phase 1, 2026-09-08 | needs `backfill_memberships.sql` STEP 1 run against staging + production first |

| 41 | **CLOSED 2026-09-08 — both halves.** Original text: the Vue staff self-service screens (My Bookings, My Earnings, My Profile, My Organization, all guarded by `staffMemberOnlyGuard`) resolved the signed-in staff member's own `providerId`, which an employee does not have. **Web half**: the four views are re-keyed onto `useMembershipStore` / `activeStaffMembership` from `GET /memberships/me`; none of them reads a `providerId` of its own any more. **Backend half, which was the larger one and was not in the original diagnosis**: the screens would have stayed broken regardless, because every provider-scoped route answered **403** for an employee. `BookingsController`, `ProvidersController` and `ProviderSettingsController` each carried a copy of the same owner-only `CanManageProvider` — "does the caller OWN this provider?" — and an employed stylist, receptionist or manager owns nothing. Replaced with a single membership-based `CanManageOrganizationQuery` carrying an explicit permission: `ManageOrganization` (profile/hours/services/team/settings) for Owner or Manager, `ManageBookings` (the day book) for any active member. 11 unit + 6 real-HTTP integration tests. `Provider.OwnerId` remains as the migration fallback (#40). | surfaced by `vue-staff-on-memberships`, 2026-09-08; closed 2026-09-08 | closed — no longer blocks dropping `ParentProviderId` |
| 42 | **CLOSED 2026-09-08.** Original text: `StaffProfile` had no per-membership working schedule or service assignments, so "works Tue–Thu here, Fri–Sat there" could not be expressed, and `MemberBookabilityService` generated every member's availability from `organization.BusinessHours` alone. Now: `StaffWorkingDay` (a `staff_working_days` table owned by `staff_profiles`) and `StaffProfile.ServiceIds` (a `jsonb` column), both empty by default, meaning "the salon's own hours" and "all of the salon's services" so the common case stays free of data. Availability generation uses the member's own day intersected with the salon's opening hours; qualification honours `PerformsService` and withdraws a member from services they no longer perform; a schedule edit regenerates their unbooked future slots (booked/held/blocked are never removed). Exposed on `PATCH /memberships/{id}` as `workingDays` / `serviceIds`, where null means "leave unchanged" and an empty list clears. 15 domain + 18 service + 4 handler unit tests, and 6 integration tests against real Postgres for the owned collection and the jsonb column. Two defects were found and fixed on the way: the per-member availability bug in #1, and services created via `POST /providers/{id}/services` being stranded in `Draft` forever (nothing qualified anyone for a service added after the members joined, and `Activate()` refuses without a qualified member) — now handled by `SyncServiceAsync`. `FindOverlappingSlotsAsync`'s own missing `StaffId` filter stays open under #1. | surfaced while reviewing the membership model against its own design doc, 2026-09-08; closed 2026-09-08 | closed |
| 43 | **`Customer.Profile` is a non-persisted duplicate.** `Customer.Create` takes a `UserProfile` and exposes it, but `CustomerConfiguration` calls `builder.Ignore(c => c.Profile)`; the persisted profile is `User.Profile` (a separate `user_profiles` row). A customer loaded from the database therefore has `Profile == null`, and `Customer.UpdateProfile`/`CustomerRegisteredEvent` read a value that only exists on the instance that created it. Found while making `CustomersControllerTests` pass 2026-09-09 — the handler that "updated the customer profile" was updating the User, and a test asserted on the ignored copy. Not fixed: the honest change is to drop `Customer.Profile` (and the ctor parameter) and route every reader through the User, which touches the aggregate's public surface and the customer read models. | found fixing the integration baseline, 2026-09-09 | small refactor change of its own; until then never read `Customer.Profile` on a loaded customer |
| 44 | **The pipeline's `TransactionBehavior` commits exactly one bounded context's unit of work — whichever registered `IUnitOfWork` last.** In `Booksy.Host` that is ServiceCatalog's (registered after UserManagement in `Program.cs`), so every UserManagement handler that relied on the pipeline lost its writes silently: customer profile/favorites/preferences, password login, refresh rotation, change-password, register, activate/deactivate, password reset (all fixed 2026-09-09 by committing `IUserManagementUnitOfWork` explicitly — 3cb5c24d, 4ebc5a15). In `Booksy.UserManagement.API` the roles flip and UserManagement's unit of work is the one wrapped, which is why a failed best-effort raw read (`MembershipInfoService`) could abort the request transaction there. The explicit commits are correct in both hosts, but the design is a trap: a new UM handler that forgets the explicit commit works in the UM host and loses data in the composed one. | found while reducing the integration baseline, 2026-09-09 | own change: make `TransactionBehavior` commit the unit of work of the handler's own context (route by handler assembly, or a composite unit of work that flushes every registered context), then delete the per-handler commits |
| 45 | **The ServiceCatalog integration suite is nondeterministic under parallel execution.** xUnit runs test classes in parallel against ONE Testcontainers database and `CleanDatabaseAsync` in the shared base is a no-op, so nothing isolates tests: booking-5c ran the full suite twice on identical code and got 60 vs 61 failures with a different flaky set each time (all 7 `ProviderCategoryPersistenceTests` in one run, `RegisterAndAcceptInvitationTests.Concurrent_*` in the other; each passes as a class). Consequences: any single full-suite number is ±several tests, `tests/known-failures.txt` can flag spurious "regressions", and a fixture that writes more data (e.g. `MakeBookableAsync` generating availability) shifts which tests lose the race without being at fault. | measured 2026-09-09 by booking-5c while reducing the baseline | **Partly done 2026-09-09 (booking-5d):** the ServiceCatalog integration assembly now carries `[assembly: CollectionBehavior(DisableTestParallelization = true)]`. Two FULL verifies in a row had failed on a different "off-baseline" test each time — `CheckSlotAvailability_ForBookedSlot`, then `BookingSlotIntegrityTests`' 40-way concurrent-insert stress test — both of which pass in isolation, and a gate that cannot tell a race from a regression is not a gate. Serialised, the suite reports exactly the 10 baseline failures and nothing else (491 passed), at a cost of 7m27s instead of 3m22s. Still open, and the real fix: per-class isolation (a schema or database per class) so the suite can be parallel AND deterministic, plus a `CleanDatabaseAsync` that cleans |

| 46 | **The application has three names for "administrator" and endpoints disagree about which one grants access.** `[Authorize(Roles = ...)]` attributes ask for `Admin` (payouts, payments, bulk notifications, UserManagement customers); the policies in `PolicyAuthorizationExtensions` ask for `Administrator`/`SysAdmin`; `ServicesController`, `ProvidersController` and `ProviderSettingsController` hedge by checking all three; `UserBuilder` mints `Administrator`. Nothing maps between them, so whether an admin can call an endpoint depends on which spelling their token happens to carry — and no seeding code in the repository grants `Admin` at all. Found because the test admin identity carried only `Administrator` and every `[Authorize(Roles="Admin,...")]` endpoint answered 403; the test user now carries all three names, which is a workaround, not the fix. | found while reducing the integration baseline, 2026-09-09 | own change, and a **security-semantics decision**: pick one vocabulary (or an explicit alias map), migrate the granted roles, then delete the hedging `IsInRole` chains. Widening what "Admin" means is not a drive-by fix |
| 47 | **Money has no rounding or per-currency precision.** `Money.Create` stores whatever decimal it is given, and `Add`/`Subtract`/`Multiply` never round, so a percentage that does not divide exactly propagates 28 significant digits into DTOs and the database — inclusive 20% tax on 120 produced a tax of `20.000000000000000000000000004`. `TaxRate` now rounds its inclusive-tax and base-amount results to 2 decimals (the precision the columns, DTOs and clients already assume), which fixes the visible case; commissions, platform fees, deposits and discounts still round nowhere. IRR, the production currency, has no minor unit at all, so 2 decimals is itself an assumption. | found while reducing the integration baseline, 2026-09-09 | own change: give `Money` a currency-aware minor unit and round at construction, then drop the local rounding in `TaxRate` |
| 48 | **CLOSED 2026-09-11 — and the diagnosis below was wrong about the mechanism, which made the fix look far riskier than it was.** The columns were never `timestamp without time zone`: every one of the 162 timestamp columns in both contexts' migrations is `timestamp with time zone` (118 ServiceCatalog, 44 UserManagement, zero exceptions), and `timestamptz` stores an absolute instant — so no stored value was wrong, no data needed converting, and no migration on a shared database was involved. The defect was entirely in the driver switch, which was set in a FOURTH place this entry missed (`PostgresTestContainerFixture`). Under it Npgsql reads a `timestamptz` back as `Kind=Local` — the right instant rendered in the machine's wall clock — and everything compared those values against `DateTime.UtcNow` or in-process instants, which is off by the UTC offset because `DateTime` comparison ignores `Kind`. (The OTP diagnostic's `…+03:30` in an `:o` format was the tell: that offset is only emitted for `Kind=Local`.) Fixed by removing the switch from all four places and adding one EF convention, `UtcDateTimeConverter`, to both contexts: modern Npgsql refuses an unmarked `DateTime`, and plenty arrive unmarked (`?date=2026-09-14` binds as `Kind=Unspecified`, as does `DateOnly.ToDateTime`), so the convention reads them as UTC — the invariant the code already assumed — instead of as local time. Also fixed a separate same-family bug: `AvailabilityService` filtered future slots against `DateTime.Now`, discarding every free slot in the next 3h30. Result: all four baseline entries pass and `tests/known-failures.txt` is empty; the OTP resend cooldown, guarded off pending this, now enforces and has tests. Change: `openspec/changes/_inline/utc-instants-end-to-end`. Original text follows. **Every UTC instant is written to Postgres shifted by the server's UTC offset.** All three hosts (`Booksy.Host`, `Booksy.UserManagement.API`, `Booksy.ServiceCatalog.Api`) call `AppContext.SetSwitch("Npgsql.EnableLegacyTimestampBehavior", true)`, and the `DateTime` columns are `timestamp without time zone`; under that switch Npgsql converts a `Kind=Utc` value to local time on write and returns `Kind=Unspecified` on read. Measured, not inferred: a notification scheduled for `18:37:28Z` was stored as `22:13:57` (+3:30, this machine's offset), and `DeliveryAttempt.NextRetryAt`, set to "5 seconds from now", lands 3.5 hours in the future — so a failed notification cannot be retried for hours. Everything that compares a stored time against `DateTime.UtcNow` — or against a time computed in process — is affected: scheduled sends, retry backoff, expiry, token lifetimes, and **availability**. The availability case is the serious one: `AvailabilityService` builds its slot grid in memory from the salon's business hours (`date.Date.AddMinutes(...)`, never round-tripped) and compares it against booking times read back from Postgres, which are shifted. A slot that is already booked therefore still reads as available — a **double-booking risk**, demonstrated by `CheckSlotAvailability_ForBookedSlot_ShouldReturnFalse` and `GetAvailableSlots_ShouldExcludeBookedSlots`, both on `tests/known-failures.txt`. #10 is the same defect measured on bookings a month earlier (a booking seeded at 10:00 reading back at 13:30). `ScheduleNotification_WithValidData` is the one line left in `tests/known-failures.txt` from this block, and it is failing for exactly this reason. | measured while reducing the integration baseline, 2026-09-09; closed 2026-09-11 | closed — no migration was needed |

| 49 | **`GET /payouts/provider/{providerId}` returns any provider's payout history to any authenticated user.** The action carries a bare `[Authorize]` and passes the route's `providerId` straight to the query — no ownership or membership check, unlike the provider-scoped reads in `BookingsController`/`ProvidersController`/`ProviderSettingsController`, which all go through `CanManageOrganizationQuery`. So a signed-in customer can enumerate provider ids and read gross, commission and net amounts plus bank-account last-4 and bank name. Pre-existing; found while reviving `GET /payouts/{id}`, which was tightened to Admin/Finance rather than left equally open. Not fixed here: the two tests covering it authenticate as the provider's owner and the fixture's owner may have no membership row, so applying the membership check blind would break passing tests, and access rules for financial data are a security decision, not a drive-by. | found while reducing the integration baseline, 2026-09-09 | own change: authorize by membership (owner/manager) or Admin/Finance, with the fixture updated to give the owner a real membership |
| 50 | **CORRECTED 2026-09-11 — the first paragraph below was wrong and is kept only so the correction is legible. `app.UseClientRateLimiting()` IS in the pipeline in `Booksy.Host` and `ServiceCatalog.Api`, fully wired (options bound from `ClientRateLimiting`, Redis-distributed store, `ClientRateLimitResolver`). What is inert is the *other* limiter: the `[EnableRateLimiting("...")]` attributes are ASP.NET Core's built-in API, and no `AddRateLimiter`/`UseRateLimiter` exists, so all thirteen named policies do nothing. Three concrete gaps, measured: (1) the OTP send handler calls `PhoneVerification.Create(...)` on every request and never loads the active verification, so the aggregate's own 60-second cooldown and 3-send cap in `CanResend()` never apply — SMS bombing a phone number is unbounded, and each message costs real money; (2) the named policies protecting OTP, password reset and registration are decoration; (3) `ClientRateLimitResolver` returns the constant string `"anonymous"` for every unauthenticated caller, so the whole internet shares ONE 100-per-minute bucket per endpoint — simultaneously useless against a single abuser and a shared-fate denial of service for everyone else.** Original, incorrect text: "No rate limiting is in effect anywhere; roughly twenty endpoints are decorated as if it were." `[EnableRateLimiting("...")]` appears on OTP send/verify (`AuthController`), registration (`UsersController`, `CustomersController`, `ProvidersController.CreateProviderDraft`), password reset and authentication (`AuthenticationController`), categories and provider availability — but `src` contains no `AddRateLimiter(...)` call and no `app.UseRateLimiter()`, so the attributes are inert metadata and no policy by those names exists. The only thing in the pipeline is `CustomRateLimitMiddleware`, which does not limit anything: it reformats a 429 into RFC 7807 Problem Details if some other component produces one, and nothing does. Practical effect: OTP sending (real SMS, real money) and OTP verification are unthrottled, so SMS-bombing a phone number and brute-forcing a numeric code are both unbounded; so is password-reset mail. | found while reading the registration endpoints, 2026-09-09 | own change, and a **tier-3 decision**: the thresholds (per phone, per IP, per account, and the OTP attempt cap) are a product/security call, not something to pick unilaterally. Whoever takes it should also decide whether the attributes' policy names are the ones to register |
| 51 | **Every `ArgumentException`, from any layer, answers 400 with its raw message.** `ExceptionHandlingMiddleware` maps the type wholesale to `INVALID_ARGUMENT`, so a server-side defect that happens to throw one reads as the caller's fault. Measured while fixing #48: Npgsql's `Cannot write DateTime with Kind=Unspecified…` (an `ArgumentException`) surfaced as thirteen 400s, which is what a client would see for a bad request — and the message leaks a driver internal. `ArgumentException` is thrown by the BCL, EF and Npgsql for programming errors far more often than by this code for input validation, which already has `DomainValidationException`/`ValidationException`. | found fixing #48, 2026-09-11 | own change: map only this codebase's own validation types to 400, let a bare `ArgumentException` be a logged 500, and check which handlers throw `ArgumentException` on purpose for input they should validate instead |
| 52 | **`IPersonAccountProvisioningService.DeleteAsync` (Host) has no production caller.** Its only caller was register-and-accept's compensating delete, removed 2026-09-11. That delete ran whenever the request that created a person lost the membership race to the request that reused it, and it deleted the account the winning request had just made a member: 5 of 10 isolated runs of `Concurrent_Register_And_Accept_…`. The port and `PersonAccountProvisioningCompositionTests`' delete case remain. | `utc-instants-end-to-end` task 7b, 2026-09-11 | delete the method and its test, or keep it for a deliberate account-deletion feature. It must never again be used to undo a person another request may already hold |
| 53 | **`CreateBookingResult.DurationMinutes`/`Currency`/`PaymentStatus`/`CreatedAt` are never assigned.** `CreateBookingCommandHandler`'s `return new CreateBookingResult(...)` only sets the constructor's positional parameters; these four are plain settable properties on the record and are never touched afterward, so every `POST /api/v1/bookings` response reports `DurationMinutes: 0` and the other three as their type defaults — for every booking, single- or multi-service, not a multi-service-specific bug. Found porting the Reqnroll "Multi-service visit sums duration and price" scenario, which is why that port asserts against the stored row (`booking.Duration`/`Services.Count`/`TotalPrice`) instead of the response DTO. | found porting `retire-reqnroll`, 2026-09-11 | own change: set the four properties in the handler (or fold them into the record's positional constructor) and add a test that would have caught this on the response itself |
| 54 | **CAP publishes integration events before the EF transaction that produced them commits.** `EfCoreUnitOfWork.SaveChangesAsync` calls `DispatchDomainEventsAsync` (which runs `IEventBus.PublishAsync` → CAP's outbox insert) *before* `_context.SaveChangesAsync`, and `ExecuteInTransactionAsync` commits the surrounding transaction only after that same `SaveChangesAsync` returns — so a domain event is handed to CAP while the aggregate's own INSERT/UPDATE has not reached the database yet, and if the transaction later rolls back (the `SaveChangesAsync` call itself throwing is the one case that's safe; anything that fails between dispatch and the outer commit is not), a subscriber can act on a state change that was never actually persisted. CAP's own outbox row is written inside the same DbContext/transaction (so *that* part is atomic with the aggregate's rows), but the in-process `InMemoryMessageQueue` transport (`CapEventBusExtensions.cs`) delivers to subscribers immediately on publish, not after commit — there is no reliable "publish after commit" boundary for this transport the way there would be with an outbox-polling transport. | `docs/TEST_ARCHITECTURE_AUDIT.md` Phase 2 slice 6, 2026-09-11 | own change: either move the dispatch call after `_context.SaveChangesAsync` (before the transaction commits, so a failed commit still rolls back cleanly since nothing was delivered) or switch CAP's transport to genuinely defer delivery until after commit |
| 55 | **`ProviderDraftCreatedEventSubscriber` and `BookingEventSubscribers` are `[ICapSubscribe]` classes with no DI registration, so CAP never invokes them.** Contrast with the two subscribers that *are* wired up the same way everywhere else in this codebase — `NotificationRetrySubscriber` (`services.AddTransient<EventHandlers.NotificationRetrySubscriber>()` in `ServiceCatalogInfrastructureExtensions.cs`) and `ProviderRegisteredEventSubscriber` (`builder.Services.AddTransient<ProviderRegisteredEventSubscriber>()` in both `Booksy.Host/Program.cs` and `Booksy.UserManagement.API/Program.cs`) — CAP's `ConsumerServiceSelector` finds subscriber candidates among registered `IServiceCollection` descriptors, not via blind assembly-wide reflection, so a class that implements `ICapSubscribe` but is never `AddTransient`/`AddScoped`'d is invisible to it. `ProviderDraftCreatedEventSubscriber` (topic `booksy.servicecatalog.providerdraftcreated`, which IS published, from `ProviderDraftCreatedEventHandler.cs`) and `BookingEventSubscribers` (topics `booksy.booking.created`/`confirmed`/`completed`/`cancelled`, none of which has any publisher anywhere in `src/` — grepped, zero hits) are both missing that one registration line, so `Booksy.UserManagement.API/Program.cs:41`'s commented-out `// .FromAssemblyOf<ProviderRegisteredEventSubscriber>()` looks like an abandoned attempt to fix this generically. Two independent reads of user booking history/upcoming-bookings query handlers already document (in their own XML comments) that they depend on a read model `BookingEventSubscribers` is supposed to maintain and currently never does. | `docs/TEST_ARCHITECTURE_AUDIT.md` Phase 2 slice 6, 2026-09-11 | own change: register both subscribers (`AddTransient`, matching the pattern), and separately decide whether the four booking topics need a real publisher in `Booksy.ServiceCatalog` (nothing publishes them today, so `BookingEventSubscribers` would still never fire even once registered) |
| 56 | **A cached "this owner has no provider" `null` on `CachedProviderReadRepository.GetByOwnerIdAsync` is never invalidated, and can't be on the InMemory cache provider even if something tried.** `ProviderCacheInvalidationEventHandler` invalidates the owner-keyed cache (`Provider:owner:*`, pattern-matched since the events it handles don't carry an `OwnerId`) on fourteen provider-*update* events (profile, booking policy, hours, gallery, location, staff, activate/deactivate, exceptions, holidays) — but on nothing at provider *creation* time, so the very first `GetByOwnerIdAsync` call for a user who hasn't registered yet (made by `RegisterProviderCommandHandler`'s "does this owner already have a provider" check, or by any endpoint reading "my provider profile") caches a `null` for `CacheSettings.DefaultExpirationMinutes` (15 minutes by default) that a successful registration moments later never clears. Independently, `InMemoryCacheService.RemoveByPatternAsync` is a hard no-op (`_logger.LogWarning("Pattern removal not supported in in-memory cache"); return Task.CompletedTask;`) — so on `Cache:Provider=InMemory` (the default outside a Redis deployment; also what `appsettings.Testing.json` requests, though the merged integration suite clears the whole `IMemoryCache` between tests via `ResetStateAsync`, which is why this never surfaces there) *no* provider-cache invalidation by pattern ever does anything, update events included. | `docs/TEST_ARCHITECTURE_AUDIT.md` Phase 2 slice 6, 2026-09-11 | own change: give `RegisterProviderCommandHandler` (or a `ProviderRegisteredEvent`) an explicit owner-cache invalidation/positive-cache-write on success, and either implement real pattern removal for `InMemoryCacheService` (a second index of keys by prefix) or stop offering `RemoveByPatternAsync` as if it does something on that provider |
| 57 | **Gallery-image integration tests leak upload directories forever; nothing ever deletes them.** Every gallery/profile-image test that succeeds writes a real file to `wwwroot/uploads/providers/{providerId}/...` (confirmed: 27 provider directories accumulated from this session's test runs alone, in a build output folder that itself gets wiped only by a full `bin`/`obj` clean) and no test, fixture, or `ResetStateAsync` step ever removes them — `DatabaseReset` only ever touches Postgres tables. Over the life of this repository's CI/local runs this reaches four-figure directory counts (the audit that first raised this measured 1,099+). Harmless to correctness (the DB reset means no test can see a stale file's *row*), but it is unbounded disk growth on every machine and CI runner that runs this suite, and 1,000+ tiny directories themselves cost real time to enumerate/delete once someone does clean them. | `docs/TEST_ARCHITECTURE_AUDIT.md` Phase 2 slice 6, 2026-09-11 | own change: have the gallery/profile-image test base delete `wwwroot/uploads` (or a per-run subfolder of it) in a `[CollectionFixture]`/`IAsyncLifetime.DisposeAsync`, or point the test host's upload root at a temp directory instead of the shipped `wwwroot` |
| 58 | **SECURITY: sandbox OTP is enabled on the public production host.** `/opt/booksy/.env` on 194.1.155.230 sets `OTP_SANDBOX_CODE=222222` and `Sms__SandboxMode=true`, so `222222` is a valid OTP for **any** phone number on `https://back.nahalkmi.ir` / `https://provider.nahalkmi.ir`, and no real SMS is ever sent. Anyone who learns the code can sign in as any user, including a provider owner. `OtpCode.Generate()` reads it straight from the process environment, and its own comment says the variable is never set in production. Enabled deliberately on 2026-09-18 to test the first deployment. | first production deployment, 2026-09-18 | **before any real user signs up:** delete both lines from `/opt/booksy/.env`, then `docker compose -f docker-compose.prod.yml up -d`, then confirm a login with `222222` is rejected |
| 59 | **CI deploy job fails at `scp`, so production is deployed by hand.** `deploy.yml`'s test, keystone-E2E and both image-build jobs are green (first all-green run #66), and its 'Test SSH connectivity' step (a bare `nc -z` to port 22) passes — but 'Copy deployment files to server' (`appleboy/scp-action`), the first step that authenticates with the repo secrets, exits 1 and the check-run annotation says only 'Process completed with exit code 1'. Secrets are set as **repository** secrets (the job's `environment: production` has none of its own; the stale `SERVER_*` set on the `github-pages` environment is unrelated and unused). The only key the server accepts for `booksy` is the CI deploy key, fingerprint `SHA256:WEyLHDWneSNLYABT5Py2NzurMjRpGeJ+t2SpaBLhYWM`. **Root cause found 2026-09-18: the runner's SSH never reaches `sshd`.** `/var/log/auth.log` (6 days, 2026-09-13..18) holds sshd events from Iranian ranges only (37.156.x, 185.255.x, 37.32.x, 85.9.x, 185.231.x) — not one GitHub runner connection, and not even the internet-wide scanner noise every globally reachable port 22 gets — across the failed deploy runs of 22b91d8b and 20405675. The host firewall is open (`INPUT ACCEPT`); the filtering is upstream (the box appears to be on an Iranian network: the operator's split-tunnel VPN reaches it from an Iranian source IP). The passing `nc -z` step proves only that something answered the SYN, not that sshd did. So no secret fix can make push-based SSH deploy work. **RESOLVED 2026-09-18:** runner `booksy-prod-1` registered (systemd, enabled); run for 6593f087 went green end to end, including Deploy to Production Server, both public URLs 200. Was: `deploy.yml`'s deploy job now runs on a self-hosted runner on the box (`[self-hosted, booksy-prod]`), and the provider web app is built and published by CI (runbook › CI/CD status). | first production deployment, 2026-09-18 | close once the runner is registered and a push to `master` deploys unattended. Was: switch to a deploy the server pulls: a self-hosted GitHub Actions runner on the box (outbound only; the box already reaches ghcr.io) or a scheduled `docker compose pull && up -d` as `booksy`; then confirm a push to `master` deploys unattended. Then add the provider app (Flutter web build + upload to `/var/www/booksy-provider`) as a CI job, which does not exist yet |
| 60 | **The customer app still geocodes from the browser.** `booksy-customer-app/lib/core/location/geocoding_service.dart` calls `nominatim.openstreetmap.org` directly — the exact failure the provider app hit in production on 2026-09-19 (it fails on networks that cannot reach that host, and every visitor is an unidentified client of a shared free service). The server-side endpoints already exist: `GET /api/v1/Geocoding/search|reverse`. | geocoding-through-api, 2026-09-19 | point that service at the API endpoints, as `booksy-provider-app` now does; it is not deployed yet, so nothing is broken for users today |
| 61 | **Registration hands back its own activation token.** `POST /api/v1/Users` (anonymous) returns `activationToken` in the response body, and `POST /api/v1/Users/{id}/activate` accepts it — so anyone can register any email address and activate it without ever receiving that email. Found while creating the admin account on 2026-09-19 (the activate endpoint itself requires a signed-in caller, which is why the admin account had to be activated in the database). | admin-panel-deploy, 2026-09-19 | stop returning the token (send it by email/SMS only); decide whether email ownership matters for any role before real users arrive. Security semantics: needs the product owner's call on the verification channel |
| 62 | **The admin panel calls endpoints the backend does not have.** Probed with a real admin token on production (2026-09-19): all eight `/analytics/*` endpoints behind the dashboard, `GET /Users/me`, `PUT /Users/{id}` (backend has `PUT /Users/{id}/profile`), `PATCH /Users/{id}/toggle-status` (backend has `activate`/`deactivate`) and `POST /Users/{id}/reset-password`. The admin now lands on the providers list, which works, as do provider details, approval and gallery. | admin-panel-deploy, 2026-09-19 | decide which admin features matter first (dashboard analytics is a backend feature to build; the user-management calls are client/API mismatches to reconcile) |
| 63 | **Booking times are salon wall-clock values with no timezone.** `/Bookings/available-slots` lists `…T09:00:00` (no offset) and `CreateBooking` checks the start against business hours as that wall time; the storage layer writes such values as UTC. It works while every client echoes the listed value back unchanged — the provider app did not (it sent `.toUtc()`, so 09:00 Tehran became 05:30Z and was refused as outside 09:00–18:00; fixed client-side 2026-09-19). A client in another timezone, or any code that treats these as real instants, will be off by the offset. | admin-panel-data-fixes, 2026-09-19 | decide the model: keep wall-clock and make the API say so (e.g. a `timeZone` field / no `Z` ever), or move to real instants with a provider timezone (Asia/Tehran) and convert before the business-hours check |

## Deployment steps (not code)

> ⚠️ **D3 is now ORDER-CRITICAL and time-boxed.** Migrations auto-apply at host startup
> (`Database.Migrate()`), and the already-merged `RemoveProviderHierarchy` migration DROPS
> `Providers."ParentProviderId"` — the exact column both D3 scripts read (12 references
> each). Deploying this branch to an environment that still holds legacy sub-provider rows
> destroys the mapping before the backfill can use it, and those staff and their bookings
> can no longer be reattached automatically. **Run D3 against each environment before this
> branch is deployed to it**, or confirm the environment has no sub-provider rows at all.

| # | Item | Came from |
|---|---|---|
| D1 | **Pre-deploy overlap audit.** `EXC_Bookings_Staff_NoOverlap` fails to create if production holds overlapping active bookings for one staff member. Audit before the migration runs. | `booking-slot-integrity` §1.3 |
| D2 | **Pre-deploy duplicate-payment audit.** `UX_Payments_OneCapturedPerBooking` fails to create if any booking already has >1 captured payment. Query `Payments` grouped by `BookingId` where `Status IN ('Paid','PartiallyPaid')`. | `payment-consistency-and-idempotency` §3.1 |
| D3 | **Membership migration, in this order, per environment.** (1) `backfill_memberships.sql` — owner memberships, real-person sub-provider staff, and STEP 2b converting synthetic sub-providers into unclaimed memberships. (2) `rekey_bookings_to_memberships.sql` — rewrites `Bookings."StaffId"`, `ProviderAvailability`, and `Services."QualifiedStaff"` off sub-provider ids. (3) Run its VERIFICATION queries; the "still pointing at a legacy sub-provider" count must be 0. **2026-09-08: the code side of this has already shipped** — the sub-provider fallback and `Providers."ParentProviderId"` were removed in `cb3e7310` (migration `RemoveProviderHierarchy`), and FOLLOW-UPS #41 is closed. So D3 is now purely a DATA step, and it must run BEFORE that migration reaches an environment holding legacy sub-provider rows: after `ParentProviderId` is dropped there is nothing left to map from. Both scripts are idempotent and were validated against a real Postgres, but neither has been run against staging or production. | `refactor-identity-and-membership` §2.7/§8.7 |
| D4 | **Phone dedupe + backfill, before §1.2's unique index.** `scripts/find-duplicate-phone-numbers.sql`, then `phone_uniqueness.sql` (STEP 0 backfill, STEP 1 report, STEP 2 index). Unrelated to D3 but shares the "run before the schema change" shape. | `refactor-identity-and-membership` §1.2/§1.3 |

Both are already in `PRODUCTION_READINESS_AUDIT.md` §Deployment checklist; repeated here so they survive the
archiving of their source changes.

## #65 The customer app's generated code cannot be regenerated on the pinned SDK
`dart run build_runner build` fails in booksy-customer-app: retrofit_generator 8.2.1 does not
compile on Dart 3.12 (`lib/src/generator.dart`: "Final variable 'mapperCode' must be assigned").
The generated `*.g.dart` files are therefore tracked in git (2026-09-20) so CI can analyze, build
and deploy. Upgrade retrofit_generator (or pin an SDK it supports) and then decide whether to go
back to generating them in CI.

## #67 The notification background services are registered in a method nobody calls

`ServiceCatalogApplicationExtensions.AddNotificationBackgroundServices` registers
`NotificationProcessorService`, `ScheduledNotificationService` and `NotificationCleanupService` as hosted
services — and nothing in the host ever calls that method (grep for callers returns only the declaration).
So none of those three has ever run in any environment. This is also why
`ProcessScheduledNotificationsJob` has no caller: the service that would have driven it is itself dead.

Found 2026-09-20 while wiring the notification outbox, when a scoped service registered in the same method
failed to resolve at runtime.

Consequences today: queued notifications are never swept by `NotificationProcessorService`, scheduled ones
are never swept by `ScheduledNotificationService`, and nothing is ever cleaned up. The
`notification-system` change does not depend on any of them — its own `NotificationOutboxService` is
registered in the Infrastructure extension, which the host does call — so this is a pre-existing gap, not a
regression.

Do not simply call the method: it would start three services that have never executed against real data,
one of which deletes notifications. Each needs to be reviewed and tested on its own, and two of them may
turn out to be superseded by the outbox sweep.

## #66 Domain events are dispatched BEFORE the save, so a handler cannot read what triggered it

Corrected 2026-09-20 (this entry first claimed, and was then widened to claim, that ServiceCatalog
domain events are never dispatched; **both were wrong** — measured, not traced):

- A `POST /api/v1/bookings` creates two rows in `ServiceCatalog."Notifications"` (integration probe,
  0 -> 2), which only `BookingConfirmedNotificationHandler` writes. Handlers on the booking create
  path DO run.
- `TransactionBehavior` wraps commands in `ExecuteInTransactionAsync<T>` (EfCoreUnitOfWork:225),
  which calls `CommitAndPublishEventsAsync` (:242). The non-generic overload (:298) calls
  `SaveChangesAsync` (:317). Both dispatch.

The real defect is the ORDER. `CommitAndPublishEventsAsync` dispatches and only then calls
`SaveChangesAsync`, so when a handler runs, the aggregate that raised the event is **not yet in the
database**. Any handler that re-reads it by id gets null and silently does nothing — which is
exactly what happened to a booking SMS handler and to the guard that was supposed to stop the salon
owner being notified about their own walk-in (fixed 2026-09-20 by deciding from the event plus the
provider, which was committed earlier). `SaveAndPublishEventsAsync` (:368) has the opposite order — save, then
dispatch — and is called directly by roughly 35 handlers: membership, staff, provider-customer and
every UserManagement command. Those are safe. The commands that rely on `TransactionBehavior`
alone, booking and payment among them, get the defective order.

Consequences to keep in mind:
- A handler must take what it needs from the event, or read only entities committed before this
  transaction. Re-reading the subject of the event is a silent no-op.
- Dispatch before commit also means a handler's side effect (SMS, email) can escape for work that
  is then rolled back. Same family as the CAP-publishes-before-commit note.
- **UserManagement behaves differently:** `UserManagementDbContext.SaveChangesAsync` (:74) collects
  and dispatches on save (`CollectDomainEvents`, :126). The two contexts do not share an order.

Do not "fix" this by flipping the order globally: handlers have been running against pre-save state
for as long as they have existed, and some of them (ledger posting) move money. Change it
per-handler, with a test that proves what that handler now sees.
`openspec/changes/notification-system/design.md` chooses a transactional outbox, which sidesteps the
ordering entirely.

## #67 Toman prices and Rial payments have never been reconciled
Catalogue prices are Toman (`PlatformCurrency.Code = "IRT"`, 2026-09-20). The payment gateways
settle in Rial: `CreateZarinPalPaymentCommandValidator` requires `"IRR"`, and Behpardakht defaults
to it. A Toman price is ten Rial, so a deposit taken as `price` in IRR charges a tenth of what the
salon asked. Nothing is live (no gateway credentials), so no money has moved the wrong way — but
before the first real payment, someone must decide where the ×10 happens and prove it with a test.
Money semantics: not for an agent to pick.
